NEXT_PUBLIC_API_URL="http://localhost:3000"

# Mapa da entrega por quilometragem.
# Em produção use um provedor com termos para uso comercial (ex.: tiles do
# Geoapify com uma chave restrita por domínio — separada da chave de geocoding,
# que fica apenas no VersaAPI). O padrão abaixo serve para desenvolvimento.
NEXT_PUBLIC_MAP_TILE_URL=""
NEXT_PUBLIC_MAP_TILE_ATTRIBUTION=""
